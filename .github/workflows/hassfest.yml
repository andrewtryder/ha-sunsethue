name: Hassfest
on:
  pull_request:
  workflow_dispatch:
  push:
    branches: [main]
jobs:
  hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: home-assistant/actions/hassfest@master
